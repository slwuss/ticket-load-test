apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: booking-service-pdb
  namespace: ticketing
spec:
  minAvailable: 2       # always keep at least 2 pods up during node drains
  selector:
    matchLabels:
      app: booking-service
